# 验证记录 — TuiClean 0.1.0

日期：2026-09-15。环境：Node.js 24、Codex 内置 Chromium 浏览器。

## 自动验证

- `npm run verify`：38 项测试通过，TypeScript 检查通过，Chrome MV3 构建通过。
- `npm run build:firefox`：Firefox MV2 构建通过，无数据上传声明已配置。
- `npm run zip`：Chrome ZIP 生成成功。
- `npm run format:check`：源码格式检查通过。
- Python 检查：两种 manifest 引用的入口、图标和脚本文件存在；只申请 storage API 权限；作用域限 X/Twitter 域名；设置页在独立标签打开；ZIP 完整性通过。

## 测试范围

规则与误判上下文、昵称弱信号、Unicode 变形、个人关键词和域名、白名单、设置验证、备份导入确认、页面提取、引用隔离、动态加载、节点复用、恢复、暂停、禁用清理、扩展入口消息与存储更新、React 热更新清理。测试全部使用虚构账号、文字和 example.test 链接。

## 浏览器检查

独立演示与扩展共用 detector/controller。初始 6 条虚构样本得到 2 条折叠和 1 条弱提示，普通分享与科普保留。实际验证展开、作者白名单、新增样本自动处理、仅标注、个人关键词保存并生效、无效域名拒绝保存。检查桌面和 390 像素窄屏，无横向滚动。

修复开发入口重复创建 React root：组件与启动文件拆分后，在新标签实际触发热更新，错误及警告日志为空。

## 尚未验证

- 尚未在已登录 X 的真实信息流安装并实测；DOM 选择器仍需实际适配验证。
- Firefox 仅做构建验证，未做浏览器实测；未签名、未上架商店。
- 没有准确率基准，当前是保守启发式规则。
- 没有图片/视频识别或账号拉黑功能。
