# TuiClean 隐私说明

适用版本：0.1.1。更新日期：2026-09-16。

## 本地处理

TuiClean 在当前浏览器中读取已加载帖文的文字、显示名称、@用户名、帖文标识及可读取链接，以进行规则匹配。正文、显示昵称及正文链接用于当前页面识别，均不上传。开启拦截历史后，命中项会额外保存文字正文快照。私信路由不进行帖文识别。

本地存储保存开关、处理方式、作用范围、自定义关键词、域名、作者白名单、本地黑名单、用户名匹配规则及停用规则。黑白名单包含用户主动保存的公开 @用户名。这些信息在扩展的 `storage.local` 中保存，不使用浏览器同步存储。

拦截历史默认开启，保存命中项的帖文编号、@用户名、首次记录时间、分类、规则、原因、折叠/标注状态和文字正文快照。同一帖文去重，最多保留 1000 条或 4 MB，超过上限淘汰较早记录。单条正文最多保存 5000 个字符，超长会显示截断说明。文字按页面可读取的原样保存，其中可能包含文字形式的链接，但不会自动打开或作为 HTML 执行。不保存显示昵称、图片、视频或页面 HTML；旧记录没有原文快照。原帖链接根据账号和帖文编号生成。

可在“拦截记录”关闭新增记录或清空已有记录；清空不影响黑白名单和其他设置。历史保存在扩展的 storage.local 中，重启浏览器后保留，卸载扩展后删除。仅导出设置不包含历史；用户主动导出的完整备份会包含历史及原文，不会自动同步或上传；未命中内容不进入历史。

页面内的“展开”与“暂停”只存在内存里。关闭页面或刷新后消失。

## 网络、凭证与权限

文字过滤和本地名单不发起额外网络请求，不包含遥测、广告追踪或外部模型调用。用户主动发起 X 拉黑时，扩展才通过 X 网页内部接口读取对应原帖并提交拉黑请求，相关数据只发送到当前 X / Twitter 站点。

用户点击“更新规则”时，扩展会从本项目 GitHub 仓库的固定地址下载规则数据：`https://raw.githubusercontent.com/murongg/TuiClean/main/rules/builtin.json`。请求不附带 Cookie、X 授权信息、帖子、个人名单或自定义规则，也不发送来源页面地址。GitHub 可能接收到 IP 地址等建立网络连接所需的信息。没有定时后台检查；不点击更新时，过滤使用已缓存或随扩展提供的规则。

规则包仅包含词表、规则说明、版本和日期。更新成功后，数据包及最近检查／应用时间保存在本机；失败时保留原规则，可通过“恢复内置规则”删除规则缓存。规则缓存与个人设置、历史记录分开保存，不纳入个人数据备份。不会下载或执行远程脚本。

扩展声明 `storage`、`scripting` 及 `x.com`、`twitter.com`（含 www）和 `raw.githubusercontent.com` 的 HTTPS 站点访问权限。`scripting` 用于在专用 X 工作页调用接口执行器；GitHub 域名权限只用于用户主动下载固定地址的规则包。不请求 `cookies`、`history`、`webRequest` 或全站权限。

Firefox 构建额外声明 `authenticationInfo` 和 `websiteActivity` 数据类型，用于说明用户主动发起的 X 授权请求及账号操作。这不表示数据会发送给 TuiClean 或第三方分析服务。

请求采集只在带有 TuiClean 工作标记的 X 页面启动，普通 X 浏览页不安装请求拦截。采集器只观察同源帖文详情（TweetDetail）的 fetch / XMLHttpRequest，临时使用必要的 Authorization、CSRF 及少量 X 客户端请求头和查询模板，不观察私信接口，也不直接读取 Cookie 或密码。凭证仅保留在工作页的闭包内存中，十五分钟后不可用，离开或关闭工作页时清理，不传给扩展后台、不写日志、不保存到 storage.local、不纳入任何备份。Cookie 由浏览器在同源请求中按当前登录态自动附带。

发起拉黑前，执行器读取原帖接口响应，核对帖文编号、当前帖文作者的 @用户名及数字用户 ID，再向 X 提交拉黑。只有账号身份匹配且接口明确确认，或读取复核确认已拉黑时才报告成功。登录上下文改变、请求被拒绝、限流或结果不明确时停止后续处理，不自动重试不确定的 POST。

X 拉黑由后台串行执行，操作间隔至少两秒。任务仅在内存中运行，不自动恢复。取消、关闭调用页面或连接断开会停止后续请求，并关闭扩展自己创建的工作页；已发送给 X 的请求可能已生效，停止不会撤销它。X 黑名单不自动导入本地名单，导入备份和规则匹配也不会触发 X 拉黑。

X 网页内部接口不是稳定的公开开发者 API，接口、认证要求和限流策略可能变化。该说明仅描述 TuiClean 的行为，X 网站自身仍会正常联网。

## 用户控制

可在设置中关闭过滤、编辑本地黑白名单与用户名规则、导出备份或恢复默认。本地拉黑可以直接取消；恢复默认或卸载扩展不会撤销已提交给 X 的拉黑，需在 X 中自行取消。设置备份含个人规则与用户名；完整备份还包含拦截记录及其原文，请自行保管。导入前展示内容摘要，由用户点击应用；配置会替换，历史会与现有记录合并、按帖文去重并按空间与条数限制保留较新的记录。导入不会执行 X 拉黑。卸载扩展会移除其浏览器本地存储；日常更新应覆盖原加载目录并重新加载扩展，无需卸载。

体验页内置虚构样本，也接受用户主动输入的文字、昵称与账号。这些输入、演示设置和演示拦截记录仅保留在当前页面内存中，可清空，刷新后消失；不保存、上传或自动访问其中的链接，不会更改已安装扩展的实际偏好。
