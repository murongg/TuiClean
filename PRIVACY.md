# TuiClean 隐私说明

适用版本：0.1.0。更新日期：2026-09-15。

## 本地处理

TuiClean 在当前浏览器中读取已加载帖文的文字、显示名称、@用户名、帖文标识及可读取链接，以进行规则匹配。该处理是临时的；页面内容与识别结果不会上传或持久保存。私信路由不进行帖文识别。

本地存储仅保存开关、处理方式、作用范围、自定义关键词、域名、作者白名单、本地黑名单、用户名匹配规则及停用规则。黑白名单包含用户主动保存的公开 @用户名。这些信息在扩展的 `storage.local` 中保存，不使用浏览器同步存储。

页面内的“展开”与“暂停”只存在内存里。关闭页面或刷新后消失。

## 网络、凭证与权限

本版本没有业务网络请求、遥测、广告追踪或外部模型调用，也不读取 Cookie、密码、私信或浏览历史。不请求 `cookies`、`history`、`webRequest` 或全站访问权限。

页面脚本只能在配置的 `x.com`、`twitter.com` 及其 `www` 主机运行。读取当前活动标签标识仅用于向本扩展页面脚本请求计数和暂停状态，不保存标签列表。

用户主动点击“X 拉黑”后，扩展会操作 X 原生菜单及确认按钮，由已登录的 X 页面提交账号拉黑请求。扩展核对菜单和确认界面中的目标用户名，但不读取登录凭证，不直接调用私有接口。规则匹配不会自动触发账号拉黑，X 黑名单不会导入扩展的本地名单。

X 网站本身仍会正常联网；本声明仅描述 TuiClean 的行为。未来若增加远程规则、云端模型或社区上报，必须另行更新功能与隐私说明。

## 用户控制

可在设置中关闭过滤、编辑本地黑白名单与用户名规则、导出备份或恢复默认。本地拉黑可以直接取消；恢复默认或卸载扩展不会撤销已提交给 X 的拉黑，需在 X 中自行取消。备份含个人规则与用户名，请自行保管。卸载扩展会移除其浏览器本地存储。

体验页内置虚构样本，也接受用户主动输入的文字、昵称与账号。这些输入和演示设置仅保留在当前页面内存中，可清空，刷新后消失；不保存、上传或自动访问其中的链接，不会更改已安装扩展的实际偏好。
