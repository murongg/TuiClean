# 安全说明

TuiClean 0.1.1 是本地阅读辅助工具，不是安全检测服务。规则可能误判或漏判，不能保证钓鱼链接或诈骗内容被识别。

项目未设置专用安全邮箱。公开仓库后，可由维护者启用 GitHub 私密漏洞报告。涉及凭证、个人信息或可利用漏洞时，请使用仓库可用的私密报告渠道；不要把敏感材料放进公开 Issue。

当前边界：不执行远程代码、用户自定义正则或自动举报。X 账号拉黑仅由用户主动点击触发，通过 X 网页接口提交；本地规则匹配不会自动拉黑 X 账号。用户输入和页面文字使用文本方式展示，不作为 HTML 执行。备份文件会验证版本、字段、条目及大小。

联网规则仅从固定 HTTPS 地址按需下载，禁止跳转，限制响应大小和超时，验证格式、版本及字段后才替换缓存。词条按字面文字处理，匹配语法与组合判断保留在扩展内；规则包不能提供 JavaScript、任意正则表达式或新的执行指令。失败时保留原规则，用户可恢复随扩展提供的规则。
